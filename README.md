## Setup environment

```
git clone git@github.com:debleena89/KG.git
cd KG
uv init && uv venv && uv sync
cd .env.example .env
```

## Copy your API Keys inside .env

## Change RTL Folders inside `script.sh`

## Run Script: `./script.sh`